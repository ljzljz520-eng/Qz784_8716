package reporting

import "frontend_go/domain"

func Group1(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group2(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group3(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group4(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group5(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group6(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group7(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group8(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group9(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group10(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group11(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group12(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group13(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group14(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group15(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group16(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group17(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group18(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group19(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group20(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group21(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group22(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group23(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group24(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group25(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group26(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group27(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group28(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group29(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group30(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group31(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group32(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group33(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group34(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
func Group35(rs []domain.Record) map[string][]domain.Record {
	out := map[string][]domain.Record{}
	for _, r := range rs {
		out[r.Status] = append(out[r.Status], r)
	}
	return out
}
